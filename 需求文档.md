# Skills 工具包需求

自己用的 AI 编码辅助工具包。从 Trellis 只取「规范按需注入」，从 Comet Native 只取「约束结果、不绑死方法」。不搬 Trellis 三阶段状态机，不搬 Comet Classic（OpenSpec + Superpowers），不做会话记忆。

对照：

| 参考 | 采用 | 不采用 |
|------|------|--------|
| Trellis | index 指向规范正文；Checklist / Quality Check；AI 建议规范、人拍板 | Plan→Execute→Finish、每轮 breadcrumb、journal / `tl mem` |
| Comet Native | 强模型少塞流程；要结果契约（PRD、验收、归档），不要实现方法论 | Native/Classic 双产品、阶段 Guard、Builder/Verifier 平台 |
| Comet Classic | — | OpenSpec 变更树、Superpowers 技能链 |

第一期交付是一组 Skill 加少量 Node 脚本（列规范、记命中、初始化工作路径），不是 CLI 平台。

---

## 1. 改动分三档

模型先按下面归类。琐碎/普通可自判，模糊则升一档。**大改动必须先给出结论和理由，等人确认**再写 PRD。

| 档 | 什么情况 | 规范 | PRD | 任务 |
|----|----------|------|-----|------|
| 琐碎改动 | 错别字、格式、注释、明显一行修复 | 不加载 | 无 | 不建 |
| 普通改动 | 真改代码，但不改业务流程/代码行为/上下游 | 加载相关规范 | 无 | 仅当估计**本会话做不完**才建 |
| 大改动 | 改业务流程、代码行为、或影响上下游 | 执行时加载 | 必有，当验收依据 | **必建**（为验收和归档，与能否本会话做完无关） |

一条路径，不用精简/完整两套入口。不规定必须 TDD、brainstorm 或分阶段怎么写代码。

---

## 2. 规范自动加载

规范住在**业务仓**，跟代码一起版本化。工具包里不放项目规范。

### 目录

```
<业务仓>/.agents/specs/
  frontend/index.md    # 前端层
  backend/index.md     # 后端层
  common/index.md      # 通用层
  frontend/*.md
  backend/*.md
  common/*.md
  .hits.json           # 命中记录，gitignore
```

三层固定，不再按包拆。每份 `index.md` 必须有两段：

- **何时读哪些规范**（对应 Trellis Pre-Development Checklist）
- **改完对照什么**（对应 Quality Check）

index 只是路由，真正约定写在同目录其它 `.md`。

### 加载时机

不在会话开始时塞。准备写代码且不是琐碎改动时，才调用 `load-specs`。

脚本扫描三层 `index.md` 及其指向的规范文件，打成**当次**扁平编号表。模型可多选，例如 `1,3,7`。脚本返回选中文件路径并给这些路径的命中 +1。

命中按**文件路径**累计，不按菜单序号（加一个文件，序号会变）。`.hits.json` 不进 git。人定期看低命中，决定旧规范是否删或改；AI 可以建议，不能擅自改规范正文。

没有 Node 时，Skill 要求模型按同一扫描规则自己列编号表，并按同一 JSON 形状改 `.hits.json`。

### 和现有 Trellis 的关系

第一期不提供从 `.trellis/spec` 迁到 `.agents/specs` 的脚本。旧仓两套可以并存。

---

## 3. PRD

大改动用 `grill-with-docs` 访谈，整理成 PRD。后续对照 PRD 验收，不对照聊天记录。

PRD 至少写清：

- 要解决什么、不做什么
- 已拍板的决策和理由
- 可观察的验收项
- 影响范围（功能、上下游）

访谈过程中的术语和难逆转决策，按 grill-with-docs 写到该业务仓或工具包的 `CONTEXT.md` / `docs/adr/`（视决策属于项目还是工具包）。

---

## 4. 任务

跨会话靠磁盘上的任务目录，不靠记忆系统。不维护「当前任务」指针；续跑时看 `.work/` 里有哪些目录。

### 进行中

```
<业务仓>/.work/<短名>/
  prd.md       # 仅大改动
  design.md    # 大改动必有；普通+任务可省略
  task.md      # 目标、影响范围、拟改文件、进度、验收项
```

`.work/` gitignore。可以同时存在多个任务目录。

`task.md` 最低字段：

- 目标（大改动指向 `prd.md`）
- 设计方案（或指向 `design.md`）
- 影响范围
- 拟改 / 已改文件
- 进度
- 验收项（大改动来自 PRD；普通改动当场写几条可勾的）

没有 `implement.md`，不把实现步骤写成强制流程。

### 完成与归档

一次 git commit **不等于**任务完成（会分批提交）。验收项勾完之后，**你明确说「归档」**才搬。

整夹从 `.work/<短名>/` 移到：

```
~/agent-archive/<项目名>/<短名>/
```

Windows：`%USERPROFILE%\agent-archive\<项目名>\<短名>\`

`<项目名>` 用业务仓目录名。归档后可定期清理。日后用来回答：当时怎么做的、影响哪些功能、改动范围多大。

`archive-task` 收尾时问一句：有没有新约定值得写进规范？AI 只提建议，人决定写不写、写哪一层。

---

## 4.1 init 与 CodeGraph

进业务仓先跑 `init`：建工作路径（不覆盖已有规范 index），并处理 CodeGraph。

- 已有 `.codegraph/`：不重建
- `codegraph` 可用、只缺一个仓的索引：在该仓执行 `codegraph init`
- 业务仓下有多个 git 仓缺索引：列出方案（只建哪个 / 全建 / 跳过），等人选
- 命令不可用：问是否 `npm i -g @colbymchenry/codegraph`；不装则跳过索引，工作路径仍然算成功
- 不执行 `codegraph install`（MCP 接线不属于本工具包）
- 把 `templates/AGENTS.md` 追加进业务仓根目录 `AGENTS.md`：没有就创建；已有则追加；已有 `<!-- AGENT-SKILLS:START -->` / `END` 则只更新这一段。不覆盖标记外的项目说明，不写 `CLAUDE.md`

---

## 5. Skill 与安装

### 第一期 Skill

| Skill | 做什么 |
|-------|--------|
| `init` | 建工作路径；把 Skill 路由块追加进 `AGENTS.md`；检查/创建 CodeGraph 索引（多仓先给方案；CLI 没有则询问是否安装，不装则跳过） |
| `grill-with-docs` | 大改动访谈，产出 PRD，顺手维护术语/ADR |
| `load-specs` | 扫 index、列编号、读选中规范、记命中 |
| `start-task` | 在 `.work/<短名>/` 建任务文件 |
| `archive-task` | 搬到归档区，并询问是否更新规范 |

规范更新不单独做 Skill，挂在归档收尾。进业务仓后先跑一次 `init`。

### 安装

工具包（本仓库）是源码，Skill 在根目录 `skills/`。用时**复制**到业务仓 `.agents/skills/`，不 submodule、不依赖 Windows symlink 指向工具包。复制后业务仓应直接是 `.agents/skills/init/SKILL.md`。

业务仓内 Skill 正文只维护在 `.agents/skills/`。给 Cursor / Claude Code 做 symlink：

- `.cursor/skills/<name>` → `../.agents/skills/<name>`（按实际相对路径）
- `.claude/skills/<name>` → 同上

不把 `.cursor` / `.claude` 当源。第一期不安到 `~/.agents/skills/`。

列索引、记命中：Node.js 标准库脚本，放在 `load-specs` 的 `scripts/` 下。工作路径与 CodeGraph 探测：`init` 的 `scripts/init.mjs`。

---

## 6. 业务仓落地后的树

```
<业务仓>/
  .agents/
    skills/           # 从工具包根目录 `skills/` 拷来
      init/
      grill-with-docs/
      load-specs/
        scripts/      # Node：列编号、写命中
      start-task/
      archive-task/
    specs/
      frontend/
      backend/
      common/
      .hits.json      # gitignore
  .work/              # gitignore，进行中任务
  .codegraph/         # gitignore，CodeGraph 索引（init 按需建）
  .cursor/skills/     # symlink → ../.agents/skills
  .claude/skills/     # 同上（若使用 Claude Code）
  AGENTS.md           # init 追加托管路由块，不覆盖原有段落
```

---

## 7. 明确不做

- 会话记忆、journal、跨会话自动回忆
- OpenSpec、Superpowers、以及把二者串起来的阶段工作流
- Trellis 式强制 Plan / before-dev / 每轮 workflow-state
- 独立 CLI、hook 状态机、「当前任务」运行时
- 中央规范库（规范不放 my-skills）
- 第一期从 `.trellis` 自动迁移
- 用整份 `AGENTS.md` 覆盖项目原有说明，或第一期同时维护 `CLAUDE.md`

---

## 8. 第一期验收

1. 在一个业务仓拷入 Skill，跑 `init` 后工作路径齐，根目录 `AGENTS.md` 含 `<!-- AGENT-SKILLS:START -->` 路由块（原有段落仍在），Cursor 能经 junction 调到 `.agents/skills`。CodeGraph：单仓可自动 `codegraph init`；多仓先给方案；无 CLI 时询问，不装则跳过。
2. 非琐碎改动调用 `load-specs`：出现三层扫出来的编号表，多选后能读到正文，`.hits.json` 对应路径 +1。
3. 大改动：确认 → grill 出 PRD → `.work/<短名>/` 含 `prd.md` / `design.md` / `task.md` → 对照 PRD 验收 → 你说归档后目录出现在 `~/agent-archive/<项目名>/`，业务仓 `.work/` 里不再有它。
4. 普通改动本会话能做完：加载规范、不建任务、不写 PRD。
5. 不出现 Trellis 任务状态机，不出现 OpenSpec 目录，不写 journal。





# 08-19 二期需求

相对第一期的增量。业务仓里抢触发的旧 Skill 已另行处理，**本期只改工具包自身**（`skills/` 正文、`description`、`init` 写入的 `AGENTS.md` 路由块）。不在工具包里做「扫描/停用业务仓其它 Skill」。

来源：yx-mom 异常增强复盘。现场有 `.work/` 和 PRD 文件，但 Cursor **没有注入** `grill-with-docs` / `start-task`；模型自己 `Read` 了 `SKILL.md` 就用模板填空，没有按步骤执行。`load-specs` 能跑，是因为用户原话说了「加载规范」。

---

## 9. 触发与路由

Skill 不能只靠用户说出内部名称（`grill-with-docs`、`start-task`）。`description` 和 `AGENTS.md` 路由必须对上用户实际说法，并且 **先分档再选 Skill**。

读到 `SKILL.md` 不等于执行过该 Skill。没走完步骤（尤其是「停下来等人」）不准写产物交差。

### 9.1 `description` 按「档」写，不按业务场景写

`grill-with-docs` 是公共 Skill，触发条件是 **大改动这一档**，不是某次事故里的业务词。

yx-mom 那次用户说了「改异常 / 捕获空指针 / 全面强化」，那些只说明「用户不会说 grill-with-docs」。**不准**把它们写进 `description`：写进去会变成异常专项 Skill，其它大改动照样匹配不上。

| Skill | 认什么 | 不认什么 |
|-------|--------|----------|
| `grill-with-docs` | 改业务流程、改代码行为、影响上下游；用户说 grill、大改动、PRD；用户要建任务/写方案/对齐需求，且按分档属于大改动 | 具体业务词（异常、空指针、某模块名）；只读调查、解释代码 |
| `start-task` | 建任务、创建任务、建个任务、开任务、start-task、`.work/<slug>/` | 大改动尚未确认时抢在 grill 前面建目录、编 PRD |
| `load-specs` | 加载规范、load-specs（已够用，不为此改） | — |

「创建任务」本身不是 grill 的业务场景。它能进 grill，是因为 **9.2 先分档**：判定为大改动之后才走 grill，普通改动做不完才直接 `start-task`。

### 9.2 路由块：建任务先分档

`init` 写入业务仓 `AGENTS.md` 的托管段（`<!-- AGENT-SKILLS:START -->` … `END`）按下面改，重复跑 `init` 只更新这一段。

「创建任务 / 建任务」**不得**直接指向 `start-task`。顺序固定为：

1. 先分档（模糊升一档）。
2. **大改动**：先 `grill-with-docs`，用户确认「这是大改动」之后才 `start-task`。
3. **普通改动**且估计本会话做不完：才直接 `start-task`。
4. **琐碎改动**：不建任务。

路由表要写明：场景匹配或用户提到对应说法时，立刻 **Read 并按其步骤执行**，不要只口头答应，也不要用记忆或自拟流程替代。

### 9.3 `grill-with-docs`：未确认前禁止写 PRD

在第一期「未确认前不要写产品代码、不要 `start-task`」之上，再加一条：

- 用户还没确认「这是大改动」之前，**也不要写 `prd.md`**，不要用 `start-task` 的模板填一份 PRD 交差。
- 必须读本目录 `grilling.md`，按设计树提问；没问完不准进入写 PRD。
- 用户只说「创建任务 / 建个任务」，但改动会改业务流程、代码行为或上下游时：先给出「这是大改动」的结论和理由，**停**。确认后再访谈，访谈齐了再 `start-task`。

### 9.4 `start-task`：只建目录，不接着改产品代码

- 大改动没有 grill 产出的 PRD 时，本 Skill **不要自己编 PRD**；转去 `grill-with-docs`。
- 用户只要建任务、没说开始改代码：写出 `.work/<短名>/` 后 **停**，等人确认任务边界或明确说开工。
- 删掉「建好后继续改代码」这类把本 Skill 延伸到实现阶段的步骤。

---

## 10. 本期不做

- 不新增 `implement.md`、执行方法论 Skill、Superpowers 式技能链。
- 不扫、不停用、不删除业务仓里的其它 Skill（冲突已在业务仓处理）。
- 不把 Java/NPE/fallback 业务规则写进工具包（那是业务仓 `.agents/specs/`）。
- 对照 diff / 提交门禁（`check-change` 一类）不在本期；触发稳定后再单开。

---

## 11. 二期其它

- 规范加载做数据埋点：额外加日志，把触发上下文（user message 和 AI 响应）存下来，方便后续挖掘、优化加载时机。

---

## 12. 二期验收

1. 用户说「创建任务」且改动会改代码行为时：模型先分档并走 `grill-with-docs` 第一步（结论 + 理由 + 停），不先写 PRD、不先改产品代码、不先 `start-task`。
2. 用户只说「建个任务」、范围已是普通改动且做不完：才直接 `start-task`；写完 `.work/` 后停。
3. `grill-with-docs` 的 `description` 只描述大改动这一档（业务流程 / 代码行为 / 上下游，以及 grill、PRD、大改动、需要对齐再动手），**不含**具体业务场景词；用户不必说出技能名。
4. `start-task` 的 `description` 能对上「创建任务 / 建个任务 / 开任务」。
5. 业务仓 `AGENTS.md` 路由块经 `init` 更新后，不再把「建任务」单行映射到 `start-task` 而跳过grill。
6. 用模板直接生成 `prd.md`、却从未读 `grilling.md`、从未提问的路径，视为不合格。

---

# 08-19 三期需求：文档去 AI 味

相对二期的增量。**只改工具包自身**（`skills/grill-with-docs/` 正文与引用文件）。不在工具包里整包复制 `Reference/` 下的外部 Skill，也不新增独立「去 AI 味」Skill 让用户手动触发。

来源：`grill-with-docs` 产出的 PRD / `CONTEXT.md` / ADR 目前只有结构契约，没有文风约束，模型容易写出模板腔、表演腔、收束腔。`Reference/` 中已有三套可参考实现：

| 参考 | 路径 | 可取之处 |
|------|------|----------|
| **deslop** | `Reference/deslop/` | 「在说事还是在表演聪明」；默认改句而非保句；压缩标点（`——`、`、`、句中`：`）拆成独立句或真列表；中文文档默认删 coaching 式「你」；两条 nofluff 检查（删了是否丢信息 / 能否落到具体事实）；改写前后可数指标（ staged reversal、破折号、顿号、句中冒号）；引用与论断不可改内容只改 register |
| **shuorenhua** | `Reference/shuorenhua/` | 场景分档（PRD/ADR 归 `docs`，默认 `minimal`）；`protected spans`（术语、系统主语、命令、接口名、已拍板决策的事实与责任主体）；Tier 1/2/3 + 改写档位 + scope（`docs` 禁止 `aggressive`）；保真回读 + 残留味回读两步；不机械同义词轮换 |
| **Humanizer-zh** | `Reference/Humanizer-zh/` | 结构反模式清单（二元对比、三段式、破折号堆砌、空总结、模糊归因、协作对话痕迹）；五条核心规则（删填充、打破公式结构、变化节奏、信任读者、删金句）；可选 5 维评分（直接性 / 节奏 / 信任 / 真实 / 精炼）作自检 |

**不采用**：deslop 的 Python `scan.py` 与完整 lexicon 作为第一期硬依赖（可二期再评估）；Humanizer-zh 的「注入灵魂 / 增加个性」导向（与 PRD 中立、可验收冲突）；shuorenhua 的 `public-writing` 场景包；把去 AI 味做成用户另说的独立 Skill。

---

## 13. `grill-with-docs` 文档文风契约

### 13.1 目标

`grill-with-docs` 在访谈对齐后写出的 **PRD、`CONTEXT.md`、ADR**，读起来像人写的技术文档，不像模型模板填空。要求：

- **保信息**：决策、范围、验收项、术语定义、责任主体不能丢、不能增、不能漂。
- **去表演**：删掉开场套话、空总结、价值拔高、二元对比骨架、无源权威、工程师表演腔、过度 coaching 的「你」。
- **可验收**：文风 pass 是 grill 流程的一步，不是可选润色；有明确 protected 边界和自检项。

### 13.2 流程变更

在现有步骤 4（按模板写 PRD）与步骤 5（`start-task`）之间，**插入一步「文档文风 pass」**：

1. 访谈与术语/ADR 写法不变。
2. 按 `prd-template.md` 写出 PRD 初稿。
3. **读**本目录 `doc-voice.md`，对 **PRD 全文**做文风 pass（见 13.3）。`CONTEXT.md` / ADR 不强制 pass，但写作时仍遵守 protected spans 与 docs register。
4. 通过自检（13.4）后，再 `start-task` 落盘。pass 由 **fresh subagent** 执行（对齐 deslop 大文档审计：避免自读盲区）；仍明显 AI 味或保真回读失败 → **阻塞落盘**，不得 `start-task`。

`doc-voice.md` 从上述三套参考 **提炼** 为 grill 专用短协议（单文件为主，必要时附 `references/` 子文件），不 runtime 依赖 `Reference/` 目录。

### 13.3 文风 pass 规则（写入 `doc-voice.md` 的要点）

**场景**：固定 `docs`。PRD / ADR / CONTEXT 均按文档 register，目标语域参考 deslop：直接、具体、不端着；中文介于书面与口语之间，**不要**滑到 Mintlify 式第二人称 coaching，也**不要**滑到公众号腔。

**Protected spans**（继承 shuorenhua，grill 特化）：

- 已拍板的决策句及其理由（可改表述，不可改决策与因果）。
- `CONTEXT.md` 中 `_Avoid_` 与 canonical term。
- 接口名、模块名、配置项、命令、报错、数字与修饰对象 pairing。
- ADR 中「考虑过什么、为什么没选」的事实。
- 用户原话引用。

**必做清理**（合并三参考的交集）：

- 删 Tier-1 套话：「值得注意的是」「综上所述」「这不仅…更是…」「研究表明（无出处）」等。
- 二元对比骨架：「不是 X，而是 Y」→ 直接写 Y。
- 空总结段、段末价值拔高、段末单行 punchline。
- 中文文档：默认删非对比场景下的「你」；列表里 coaching 式「你可以…」改陈述句。
- 压缩标点：句中 `：` 引导解释、三个以上 `、` 串并列 → 拆句或改列表（deslop 规则）。
- 表演性动词/黑话：「落地、收口、赋能、抓手、闭环、兜住、打穿、命中（非测试断言语境）」→ 换成具体动作名。
- 禁止为躲重复而轮换同义词（shuorenhua）。

**默认力度**：`minimal`～`standard`；**禁止** `aggressive`。允许删整句空话、并相邻事实句；**禁止**为去味而删验收项或决策。

**两步回读**（shuorenhua）：

1. **保真回读**：protected spans、数字、主体-动作-目标、决策是否一致。
2. **残留味回读**（按需）：是否还有空收尾、narrator 腔（「这说明…」）、节奏过匀。

**可选自检**（Humanizer-zh / deslop，不强制打分交差）：写完后心算或直接性 / 节奏 / 信任 / 精炼；deslop 四指标（staged reversal、破折号、顿号、句中冒号）趋向 0。

### 13.4 验收

1. `skills/grill-with-docs/SKILL.md` 步骤含「文档文风 pass」，且指向 `doc-voice.md`。
2. 存在 `skills/grill-with-docs/doc-voice.md`，单文件可独立执行；内容覆盖 13.3，篇幅控制在可一次 Read 完（建议 ≤200 行；细则可拆 `references/doc-voice-*.md`）。
3. `prd-template.md` 或 `doc-voice.md` 中有一句提醒：**先内容对齐，再文风 pass；文风 pass 不能删验收项**。
4. 二期验收 6 仍成立；新增：**未读 `doc-voice.md` 就把 PRD 落盘** 视为不合格。
5. 抽检：同一份访谈产物，pass 前后决策与验收项逐条一致；明显 AI 套话（空总结、二元对比标题、coaching「你」）应被清掉。

### 13.5 本期不做

- 不新增 `deslop` / `shuorenhua` / `humanizer-zh` 为工具包 Skill 或业务仓必装依赖。
- 不引入 `scan.py`、lexicon TSV、50 分制强制门槛。
- 不要求 grill 访谈提问本身去 AI 味（只约束**写盘文档**）。
- 不扩展到 `task.md` / `design.md`（那是 `start-task` 模板，后续单开）。

---

## 14. 三期验收

1. 大改动走完整 grill：访谈 → PRD 初稿 → **doc-voice pass** → `start-task` → `.work/<slug>/prd.md`。
2. `doc-voice.md` 存在且被 SKILL 引用；模型路径上可见 Read 该文件。
3. 产物保真：PRD 验收项与已拍板决策在 pass 前后一致（允许改表述，不允许改含义）。
4. 产物去味：pass 后无典型 Tier-1 套话、无整段空总结、中文 PRD 无大面积 coaching「你」。
5. 工具包 `需求文档.md` 含本节（13、14），与 `skills/` 实现一致。
