# Skills 工具包需求

自己用的 AI 编码辅助工具包。从 Trellis 只取「规范按需注入」，从 Comet Native 只取「约束结果、不绑死方法」。不搬 Trellis 三阶段状态机，不搬 Comet Classic（OpenSpec + Superpowers），不做会话记忆。

对照：

| 参考 | 采用 | 不采用 |
|------|------|--------|
| Trellis | index 指向规范正文；Checklist / Quality Check；AI 建议规范、人拍板 | Plan→Execute→Finish、每轮 breadcrumb、journal / `tl mem` |
| Comet Native | 强模型少塞流程；要结果契约（PRD、验收、归档），不要实现方法论 | Native/Classic 双产品、阶段 Guard、Builder/Verifier 平台 |
| Comet Classic | — | OpenSpec 变更树、Superpowers 技能链 |

第一期交付是一组 Skill 加少量 Node 脚本（列规范、记命中、初始化工作路径），不是 CLI 平台。

---

## 1. 改动分三档

模型先按下面归类。琐碎/普通可自判，模糊则升一档。**大改动必须先给出结论和理由，等人确认**再写 PRD。

| 档 | 什么情况 | 规范 | PRD | 任务 |
|----|----------|------|-----|------|
| 琐碎改动 | 错别字、格式、注释、明显一行修复 | 不加载 | 无 | 不建 |
| 普通改动 | 真改代码，但不改业务流程/代码行为/上下游 | 加载相关规范 | 无 | 仅当估计**本会话做不完**才建 |
| 大改动 | 改业务流程、代码行为、或影响上下游 | 执行时加载 | 必有，当验收依据 | **必建**（为验收和归档，与能否本会话做完无关） |

一条路径，不用精简/完整两套入口。不规定必须 TDD、brainstorm 或分阶段怎么写代码。

---

## 2. 规范自动加载

规范住在**业务仓**，跟代码一起版本化。工具包里不放项目规范。

### 目录

```
<业务仓>/.agents/specs/
  frontend/index.md    # 前端层
  backend/index.md     # 后端层
  common/index.md      # 通用层
  frontend/*.md
  backend/*.md
  common/*.md
  .hits.json           # 命中记录，gitignore
```

三层固定，不再按包拆。每份 `index.md` 必须有两段：

- **何时读哪些规范**（对应 Trellis Pre-Development Checklist）
- **改完对照什么**（对应 Quality Check）

index 只是路由，真正约定写在同目录其它 `.md`。

### 加载时机

不在会话开始时塞。准备写代码且不是琐碎改动时，才调用 `load-specs`。

脚本扫描三层 `index.md` 及其指向的规范文件，打成**当次**扁平编号表。模型可多选，例如 `1,3,7`。脚本返回选中文件路径并给这些路径的命中 +1。

命中按**文件路径**累计，不按菜单序号（加一个文件，序号会变）。`.hits.json` 不进 git。人定期看低命中，决定旧规范是否删或改；AI 可以建议，不能擅自改规范正文。

没有 Node 时，Skill 要求模型按同一扫描规则自己列编号表，并按同一 JSON 形状改 `.hits.json`。

### 和现有 Trellis 的关系

第一期不提供从 `.trellis/spec` 迁到 `.agents/specs` 的脚本。旧仓两套可以并存。

---

## 3. PRD

大改动用 `grill-with-docs` 访谈，整理成 PRD。后续对照 PRD 验收，不对照聊天记录。

PRD 至少写清：

- 要解决什么、不做什么
- 已拍板的决策和理由
- 可观察的验收项
- 影响范围（功能、上下游）

访谈过程中的术语和难逆转决策，按 grill-with-docs 写到该业务仓或工具包的 `CONTEXT.md` / `docs/adr/`（视决策属于项目还是工具包）。

---

## 4. 任务

跨会话靠磁盘上的任务目录，不靠记忆系统。不维护「当前任务」指针；续跑时看 `.work/` 里有哪些目录。

### 进行中

```
<业务仓>/.work/<短名>/
  prd.md       # 仅大改动
  design.md    # 大改动必有；普通+任务可省略
  task.md      # 目标、影响范围、拟改文件、进度、验收项
```

`.work/` gitignore。可以同时存在多个任务目录。

`task.md` 最低字段：

- 目标（大改动指向 `prd.md`）
- 设计方案（或指向 `design.md`）
- 影响范围
- 拟改 / 已改文件
- 进度
- 验收项（大改动来自 PRD；普通改动当场写几条可勾的）

没有 `implement.md`，不把实现步骤写成强制流程。

### 完成与归档

一次 git commit **不等于**任务完成（会分批提交）。验收项勾完之后，**你明确说「归档」**才搬。

整夹从 `.work/<短名>/` 移到：

```
~/agent-archive/<项目名>/<短名>/
```

Windows：`%USERPROFILE%\agent-archive\<项目名>\<短名>\`

`<项目名>` 用业务仓目录名。归档后可定期清理。日后用来回答：当时怎么做的、影响哪些功能、改动范围多大。

`archive-task` 收尾时问一句：有没有新约定值得写进规范？AI 只提建议，人决定写不写、写哪一层。

---

## 4.1 init 与 CodeGraph

进业务仓先跑 `init`：建工作路径（不覆盖已有规范 index），并处理 CodeGraph。

- 已有 `.codegraph/`：不重建
- `codegraph` 可用、只缺一个仓的索引：在该仓执行 `codegraph init`
- 业务仓下有多个 git 仓缺索引：列出方案（只建哪个 / 全建 / 跳过），等人选
- 命令不可用：问是否 `npm i -g @colbymchenry/codegraph`；不装则跳过索引，工作路径仍然算成功
- 不执行 `codegraph install`（MCP 接线不属于本工具包）

---

## 5. Skill 与安装

### 第一期 Skill

| Skill | 做什么 |
|-------|--------|
| `init` | 建工作路径；检查/创建 CodeGraph 索引（多仓先给方案；CLI 没有则询问是否安装，不装则跳过） |
| `grill-with-docs` | 大改动访谈，产出 PRD，顺手维护术语/ADR |
| `load-specs` | 扫 index、列编号、读选中规范、记命中 |
| `start-task` | 在 `.work/<短名>/` 建任务文件 |
| `archive-task` | 搬到归档区，并询问是否更新规范 |

规范更新不单独做 Skill，挂在归档收尾。进业务仓后先跑一次 `init`。

### 安装

工具包（本仓库）是源码，Skill 在根目录 `skills/`。用时**复制**到业务仓 `.agents/skills/`，不 submodule、不依赖 Windows symlink 指向工具包。复制后业务仓应直接是 `.agents/skills/init/SKILL.md`。

业务仓内 Skill 正文只维护在 `.agents/skills/`。给 Cursor / Claude Code 做 symlink：

- `.cursor/skills/<name>` → `../.agents/skills/<name>`（按实际相对路径）
- `.claude/skills/<name>` → 同上

不把 `.cursor` / `.claude` 当源。第一期不安到 `~/.agents/skills/`。

列索引、记命中：Node.js 标准库脚本，放在 `load-specs` 的 `scripts/` 下。工作路径与 CodeGraph 探测：`init` 的 `scripts/init.mjs`。

---

## 6. 业务仓落地后的树

```
<业务仓>/
  .agents/
    skills/           # 从工具包根目录 `skills/` 拷来
      init/
      grill-with-docs/
      load-specs/
        scripts/      # Node：列编号、写命中
      start-task/
      archive-task/
    specs/
      frontend/
      backend/
      common/
      .hits.json      # gitignore
  .work/              # gitignore，进行中任务
  .codegraph/         # gitignore，CodeGraph 索引（init 按需建）
  .cursor/skills/     # symlink → ../.agents/skills
  .claude/skills/     # 同上（若使用 Claude Code）
```

---

## 7. 明确不做

- 会话记忆、journal、跨会话自动回忆
- OpenSpec、Superpowers、以及把二者串起来的阶段工作流
- Trellis 式强制 Plan / before-dev / 每轮 workflow-state
- 独立 CLI、hook 状态机、「当前任务」运行时
- 中央规范库（规范不放 my-skills）
- 第一期从 `.trellis` 自动迁移

---

## 8. 第一期验收

1. 在一个业务仓拷入 Skill，跑 `init` 后工作路径齐，Cursor 能经 junction 调到 `.agents/skills`。CodeGraph：单仓可自动 `codegraph init`；多仓先给方案；无 CLI 时询问，不装则跳过。
2. 非琐碎改动调用 `load-specs`：出现三层扫出来的编号表，多选后能读到正文，`.hits.json` 对应路径 +1。
3. 大改动：确认 → grill 出 PRD → `.work/<短名>/` 含 `prd.md` / `design.md` / `task.md` → 对照 PRD 验收 → 你说归档后目录出现在 `~/agent-archive/<项目名>/`，业务仓 `.work/` 里不再有它。
4. 普通改动本会话能做完：加载规范、不建任务、不写 PRD。
5. 不出现 Trellis 任务状态机，不出现 OpenSpec 目录，不写 journal。
