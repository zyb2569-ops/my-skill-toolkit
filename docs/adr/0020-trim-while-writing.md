# trim 边写边守，并在写设计/规范的步骤里硬挂钩

`trim-cot-leakage` 不是 doc-voice 那种写完再过一遍的 pass：当场按规则落笔，改已有文字只动这次碰到的段落。为避免只有 `SKILL.md` 却从未被 Read，`description` 和路由块写场景，「写 `design.md` / 写规范」的 Skill 步骤里写死 Read。注释和 JSDoc 没有独立入口，仍靠描述和路由。
